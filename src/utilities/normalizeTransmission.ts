
export function normalizeTransmission(input: string): string {
  if (!input) return ''


return input
/*
 lower = input.toLowerCase().trim()
 console.warn(`input value is: ${lower}`)
  // Fallback: capitalize first letter
  return lower.charAt(0).toUpperCase() + lower.slice(1)
  */
}